# Dependencies for czi2tif.py. The czifile package itself is installed in
# Docker by cloning upstream from GitHub at a pinned ref.
# czifile requires numpy>=2.1 and imagecodecs>=2026.6.6.
numpy>=2.1
imagecodecs>=2026.6.6
tifffile>=2024.8.30